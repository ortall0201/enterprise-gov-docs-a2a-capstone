# Minimal A2A VaaS Service Dependencies (Render-optimized)
# These are the ONLY dependencies actually used in the code

# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.30.1
pydantic==2.9.2

# LLM provider (only OpenAI is used)
openai==1.50.2

# Utilities
python-dotenv==1.0.0
python-multipart==0.0.12

# HTTP client (for testing)
httpx==0.27.2
requests==2.32.3

# REMOVED (not used in actual code):
# - crewai (not used - direct OpenAI calls instead)
# - crewai-tools (not used)
# - google-cloud-aiplatform (not used)
# - structlog (not used - using standard logging)
# - pytest (not needed for production deployment)
